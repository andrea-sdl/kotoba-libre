{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for local UI windows only",
  "windows": ["settings", "launcher"],
  "permissions": ["core:default"]
}
